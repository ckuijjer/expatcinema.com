{
  "name": "expatcinema.com-cloud",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "scrapers": "serverless invoke -f scrapers -l --aws-profile casper",
    "scrapers:local": "serverless invoke local -f scrapers -l --aws-profile casper",
    "playground": "serverless invoke -f playground -l --aws-profile casper",
    "playground:local": "serverless invoke local -f playground -l --aws-profile casper",
    "analytics": "serverless invoke -f analytics -l --aws-profile casper",
    "analytics:local": "serverless invoke local -f analytics -l --aws-profile casper",
    "fillAnalytics": "serverless invoke -f fillAnalytics -l --aws-profile casper",
    "fillAnalytics:local": "serverless invoke local -f fillAnalytics -l --aws-profile casper",
    "deploy": "serverless deploy --aws-profile casper",
    "deploy:ci": "serverless deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "debug": "^4.0.1",
    "got": "^11.5.2",
    "luxon": "^2.0.1",
    "p-map": "4",
    "ramda": "^0.27.1",
    "x-ray": "^2.3.4"
  },
  "devDependencies": {
    "prettier": "^2.3.2",
    "serverless": "^2.52.0",
    "serverless-plugin-monorepo": "^0.10.0"
  }
}
